services:
  iris-remote-organizer-bot:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${BOT_HTTP_PORT:-8030}:${BOT_HTTP_PORT:-8030}"
    volumes:
      - ./runtime:/app/runtime
